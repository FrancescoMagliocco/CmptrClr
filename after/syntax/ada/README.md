# Info on the `ada` syntax files
- Not sure if I should keep `adaAssignment` linked to `adaOperator` or not.  In
    the `vim` syntax file, the `vimOper` group highlights the equal sign, and
    that group is linked to `operator`.
